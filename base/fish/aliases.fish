# --- COLORING -----------------------------------------------------------------

alias grep='grep --color=always'
alias tree='tree -C'
alias less='less -R'
alias g=git

# --- LS (EXA) SHORTCUTS -------------------------------------------------------

alias ls='exa'
alias l='exa --long --git'
alias la='exa --long'
